export const PROFILE_ASSERT_OPTIONS = {
  disableAnimation: true,
  screenshotDelay: 1000,
  ignoreDiffPixelCount: '20%' as `${number}%`,
};

export const EDIT_PROFILE_ASSERT_OPTIONS = {
  disableAnimation: true,
  screenshotDelay: 1000,
  ignoreDiffPixelCount: '15%' as `${number}%`,
};
